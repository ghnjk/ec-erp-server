---
description: Vue 3 与 TypeScript 编码约定
globs: **/*.{vue,ts}
alwaysApply: false
---

# Vue And TypeScript

- Vue 页面和组件优先使用 `<script setup lang="ts">`；需要组件名时可保留一个普通 `<script lang="ts">` 导出 `name`。
- 导入顺序保持一致：Vue 核心、第三方库、项目内 `@/` 导入、类型导入。
- 组件文件使用 camelCase，组件名使用 PascalCase；目录使用 snake_case，变量和函数使用 camelCase。
- Props、Emits、接口和 API 返回值尽量补全类型；对象结构优先使用 `interface`，接口名按项目习惯以 `I` 开头。
- 避免引入新的 `any`；如果现有 API 或 TDesign 回调类型不足，可局部使用并控制范围。
- 页面表格、分页、弹窗、查询条件等状态优先使用 `ref` 或 `computed`，复用逻辑放到 `src/hooks/` 或 `src/utils/`。
- 样式放在 `<style scoped lang="less">`，优先使用 TDesign token 和已有全局样式。
